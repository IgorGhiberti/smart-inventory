version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: smartinventory-postgres
    environment:
      POSTGRES_DB: smart_inventory
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - smartinventory_pgdata:/var/lib/postgresql/data

volumes:
  smartinventory_pgdata:
